BankAppSkyPro: Система динамических правил и рекомендаций
Краткое описание проекта
BankAppSkyPro — это бэкенд-сервис для банковской сферы, реализующий систему динамических бизнес-правил и генерации персонализированных рекомендаций для клиентов. 
Сервис позволяет управлять правилами в реальном времени и интегрируется с Telegram-ботом для удобного взаимодействия.

Ключевые возможности:
Динамическое управление правилами — создание, просмотр и удаление бизнес-правил через REST API

Персонализированные рекомендации — генерация предложений на основе статических и динамических правил

Telegram-интеграция — взаимодействие с пользователями через чат-бота

Мониторинг и статистика — отслеживание срабатываний правил и метрик системы
Кэширование и производительность — оптимизация работы через Spring Cache

Архитектурная документация
Основные компоненты системы
text
BankAppSkyPro/
├── controller/           # REST контроллеры
│   ├── DynamicRuleController     # Управление динамическими правилами
│   ├── RecommendationController  # Получение рекомендаций
│   └── SimpleStatsController     # Статистика и управление системой
├── service/              # Бизнес-логика
│   ├── DynamicRuleService        # CRUD операции с правилами
│   ├── RecommendationService     # Генерация рекомендаций
│   ├── RuleEvaluationService     # Оценка условий правил
│   └── RuleStatisticService      # Сбор статистики срабатываний
├── model/                # Модели данных (DTO/Entity)
├── repository/           # Доступ к данным (Spring Data JPA)
├── rules/                # Статические бизнес-правила
└── telegram/            # Интеграция с Telegram-ботом
Технологический стек
Java 17 + Spring Boot (Web, Data JPA, Cache)

База данных: H2 (разработка), поддерживается переход на PostgreSQL

Кэширование: Caffeine

Внешняя интеграция: Telegram Bot API

Сборка: Maven

Инструкция по развертыванию
Предварительные требования
JDK 17 или выше

Maven 3.8+

Telegram Bot Token (для работы бота)

Локальная установка и запуск
Клонирование репозитория

bash
git clone https://github.com/zman43g/BankAppSkyPro.git
cd BankAppSkyPro
Настройка конфигурации

Создайте файл application-local.properties в src/main/resources/:

properties
# Telegram Bot Configuration
telegram.bot.username=YourBotUsername
telegram.bot.token=YOUR_TELEGRAM_BOT_TOKEN

# Database (H2 in-memory)
spring.datasource.url=jdbc:h2:mem:bankdb
spring.datasource.username=sa
spring.datasource.password=

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Application Info (filtered from pom.xml)
app.name=@project.name@
app.version=@project.version@
Сборка проекта

bash
mvn clean package
Запуск приложения

bash
java -jar target/bank-0.0.1-SNAPSHOT.jar
Или через Maven:

bash
mvn spring-boot:run -Dspring-boot.run.profiles=local
Конфигурация в Docker
Сборка Docker-образа

bash
docker build -t bankapp-skypro:latest .
Запуск контейнера

bash
docker run -d \
  -p 8080:8080 \
  -e TELEGRAM_BOT_TOKEN="your_token_here" \
  --name bankapp \
  bankapp-skypro:latest
Доступные API эндпоинты
Управление динамическими правилами
text
POST   /rule              # Создание нового правила
GET    /rule              # Получение всех правил
DELETE /rule/{productId}  # Удаление правила
Получение рекомендаций
text
GET /recommendation/{userId}  # Рекомендации для пользователя
Мониторинг и управление
text
GET    /rule/stats                 # Статистика срабатываний правил
GET    /rule/stats/{productId}     # Статистика по конкретному правилу
POST   /management/clear-caches    # Очистка кэшей системы
GET    /management/info            # Информация о системе (версия, uptime)
Настройка Telegram-бота
Создайте бота через @BotFather в Telegram

Получите API токен

Укажите токен в настройках приложения:

properties
telegram.bot.token=ВАШ_ТОКЕН_ЗДЕСЬ
После запуска приложения, бот будет доступен по имени, указанному при создании

Полезные команды Maven
bash
# Запуск с тестами
mvn clean verify

# Запуск без тестов
mvn spring-boot:run -DskipTests

# Генерация отчета о покрытии тестами
mvn jacoco:report

# Проверка зависимостей
mvn dependency:tree
📊 Мониторинг и логирование
Приложение предоставляет следующие точки мониторинга:

Журналирование: Логируются все операции с правилами и рекомендациями

Метрики: Счетчики срабатываний правил сохраняются в БД

Health-check: Встроенные проверки работоспособности через /management/info

Устранение неполадок
Распространенные проблемы
Бот не отвечает в Telegram

Проверьте правильность токена

Убедитесь, что приложение запущено

Проверьте настройки прокси (при необходимости)

Ошибки подключения к БД

Проверьте параметры подключения в application.properties

Убедитесь, что порт 8080 не занят

Правила не сохраняются

Проверьте формат productId (должен быть валидный UUID)

Убедитесь в уникальности productId

Лицензия
Проект разработан в учебных целях для SkyPro.

Контакты и поддержка
По вопросам работы приложения обращайтесь к разработчику или создавайте issue в репозитории проекта.